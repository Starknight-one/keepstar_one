# Sync Context

Sync all project context: expertise files + README documentation.

## Instructions

Run both commands sequentially:
1. `/cold_start` - syncs all expertise YAML files with actual code
2. `/sync-readme` - updates all README files

## What it does

1. Runs `/cold_start` - syncs all expertise YAML files with actual code
2. Runs `/sync-readme` - updates all README files
3. Reports completion status
