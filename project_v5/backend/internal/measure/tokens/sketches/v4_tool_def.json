{
  "name": "visual_assembly",
  "description": "Build or modify visual formation using ops. Insert widgets, atoms, and layout nodes to build any UI.",
  "input_schema": {
    "type": "object",
    "required": ["mode"],
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["rebuild", "modify"],
        "description": "REQUIRED. \"rebuild\" discards the previous formation and builds a fresh one from ops/preset — use when the user asks for something new (new data, detail view, empty state, different layout). \"modify\" loads the existing formation and applies ops as deltas targeting atoms by fieldName or tree ID — use for style tweaks, removals, additions on the current view (\"make price red\", \"remove rating\", \"2 columns\"). No default: pick per turn based on user intent."
      },
      "ops": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "op": {"type": "string", "enum": ["insert", "update", "delete", "move"]},
            "target": {"type": "string", "description": "Atom/node field name (applies to all widgets) or specific ID from formation_tree."},
            "ref": {"type": "string", "description": "Binding name — subsequent ops can reference via $ref."},
            "parent": {"type": "string", "description": "Parent ID or $ref for insert/move."},
            "after": {"type": "string", "description": "Insert/move after this element."},
            "props": {
              "type": "object",
              "properties": {
                "type": {"type": "string", "description": "Atom: text, number, image, icon. Layout: row, column, flow, span. Container: widget."},
                "value": {"description": "Literal value for freestyle atoms."},
                "fieldName": {"type": "string", "description": "Field name for data-bound atoms."},
                "textStyle": {
                  "type": "object",
                  "properties": {
                    "fontSize":       {"type": "string", "enum": ["xs","sm","md","lg","xl","2xl","3xl"]},
                    "fontWeight":     {"type": "string", "enum": ["light","normal","medium","semibold","bold"]},
                    "color":          {"type": "string", "description": "Semantic token or hex: red, green, blue, muted, #FF0000"},
                    "textDecoration": {"type": "string", "enum": ["none","underline","line-through"]},
                    "textTransform":  {"type": "string", "enum": ["none","uppercase","lowercase","capitalize"]},
                    "lineClamp":      {"type": "integer", "description": "Max visible lines"},
                    "lineHeight":     {"type": "string", "enum": ["tight","normal","relaxed","loose"]},
                    "letterSpacing":  {"type": "string", "enum": ["tight","normal","wide"]}
                  },
                  "description": "Text styling. Color, fontSize, fontWeight MUST be inside this object."
                },
                "wrapper": {
                  "type": "object",
                  "properties": {
                    "type":    {"type": "string", "enum": ["none","badge","tag","pill","avatar","tooltip","alert","link","progress","button"]},
                    "variant": {"type": "string", "enum": ["primary","secondary","success","error","warning","outline"]}
                  }
                },
                "mediaStyle": {
                  "type": "object",
                  "properties": {
                    "aspectRatio": {"type": "string", "enum": ["1:1","4:3","16:9","auto"]},
                    "objectFit":   {"type": "string", "enum": ["cover","contain","fill"]}
                  }
                },
                "format": {
                  "type": "string",
                  "enum": ["currency","stars","stars-compact","stars-text","percent","number","date","text"]
                },
                "slot": {
                  "type": "string",
                  "enum": ["hero","title","price","primary","secondary","badge","tags","description"]
                }
              }
            }
          },
          "required": ["op"]
        },
        "description": "Operations on the formation tree. Use WITH preset for overrides, or WITHOUT for freestyle builds."
      },
      "layout":  {"type": "string", "enum": ["grid","list","single","carousel"], "description": "Formation layout type."},
      "columns": {"type": "integer", "description": "Number of columns for grid layout."},
      "size":    {"type": "string", "enum": ["tiny","small","medium","large"], "description": "Widget size."},
      "replicate": {"type": "boolean", "description": "If true, replicate the single widget template across data items from state (one widget per product/service). Default false. Use for grids/lists of search results. Leave false for freestyle compositions, detail views, or widgets built from literal values."},
      "limit": {"type": "integer", "description": "Max number of data items to use (0 or omitted = all). Caps replication count. Ignored when replicate=false."},
      "preset": {
        "type": "string",
        "description": "Named preset — expands to a prebuilt ops batch. Global fallbacks: product_card, product_card_compact, product_card_horizontal, product_card_list_row, product_detail, product_detail_horizontal, text_explainer, empty_not_found, error_generic, catalog_category_card, liked_grid, cart_grid. Tenant-scoped presets (and overrides of the fallbacks above) are listed in the <tenant_design_context> block of the system prompt — pass those names verbatim. Pair with `ops` to apply overrides on top (update/delete/insert targeting preset refs like $w, $root, $info, $meta). Each preset has a DefaultReplicate baked in; passing `replicate` explicitly overrides it."
      }
    }
  }
}
