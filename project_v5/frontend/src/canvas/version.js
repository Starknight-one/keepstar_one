// VERSION — the builder's version string, shown in the canvas corner
// (V2_SPEC §1: "we test on prod; the footer answers what is live right
// now"). SINGLE SOURCE — nothing else may hardcode it.
//
// Bump it together with a new entry in VERSIONS.md at the repo root:
// that file is the append-only history this string points into.
export const VERSION = 'v2.0'
