# Backlog 2 — Путь к первой сделке

6 шагов к минималке, которую можно продавать. Без лишнего — только то, что блокирует продажу.

---

## Шаг 1 — Структура каталога ✅

### Собрать правильную структуру каталога
**Статус:** ✅ Полностью готов (код + данные в проде)

**Что сделано:**
- PIM-колонки в БД: `product_form`, `texture`, `routine_step`, `skin_type[]`, `concern[]`, `key_ingredients[]`, `target_area[]`, `free_from[]`, `marketing_claim`, `benefits[]`, `how_to_use`, `enrichment_version`
- Legacy дропнут: `attributes` (JSONB), `short_name`, `volume`, `inci_text`
- Volume разбит: `volume_ml` (734), `weight_g` (65), `unit_count` (962)
- Enrichment V2 прогнан: 961/962 продуктов (`enrichment_version=2`)
- Embeddings: 962/962 — все на месте
- Ingredients: 4705 ингредиентов, 27318 связей, 939 продуктов покрыты
- B-tree + GIN индексы на PIM-колонках
- VectorFilter: `KeyIngredient` и `TargetArea` добавлены (fix 2026-02-19)
- Digest tree: root-level categories fix (2026-02-19)
- Дайджест пересобран в новом формате (category_tree, shared_filters, top_brands, top_ingredients)

**Не делаем пока:**
- Enrichment agent inline в import pipeline — отложено
- Парсинг фактов (volume, INCI) в коде вместо LLM — отложено

---

## Шаг 2 — Сжатие дайджеста ✅

### Прокачать сжатие дайджеста
**Статус:** ✅ Готов

**Что сделано:**
- Новый формат: CategoryTree + SharedFilters + TopBrands(30) + TopIngredients(30)
- ~646 токенов для hey-babes (2584 символа) — вместо прежних ~2000+
- Доставка: one-time injection в `conversation_history` при `session/init`, кешируется Anthropic
- Убрана per-turn загрузка из agent1_execute
- `ToPromptText()` — ультракомпактный текст

**Может потребоваться:**
- Дальнейшее сжатие если токен-бюджет станет критичным (сейчас ок)

---

## Шаг 3 — Сжатие контекста истории сообщений

### Убрать дыру в 80k токенов при наличии истории
**Статус:** 🔍 Исследование завершено, нужны замеры

**Контекст:** Цифра 80k токенов — из старого формата дайджеста (до редизайна). После compact digest (~650 токенов) проблема может быть уже решена. Нужны замеры на живых запросах.

**Текущая архитектура (что подаётся агентам):**

**Agent1 получает:**
1. `Agent1SystemPrompt` — ~34 строки правил
2. `toolDefs` — catalog_* + 20 padding tools (~5500 токенов) → `cache_control` на последнем
3. `ConversationHistory` — полная история из БД:
   - Digest seed: `user: <catalog>...~650 tok...</catalog>` + `assistant: ok`
   - Каждый ход: `user: запрос` → `assistant: tool_use(JSON)` → `user: tool_result("ok: found N")`
   - `cache_control` на предпоследнем сообщении (инкрементальный кэш)
4. Enriched query: `<state>{мета ~100-200 tok}</state>\n\nзапрос` (или голый запрос без данных)

**Agent2 получает:**
1. `Agent2ToolSystemPrompt` — ~66 строк (рус)
2. `toolDefs` — render_* и freestyle
3. Последние 4 user-сообщения из истории (без tool_use/tool_result)
4. `BuildAgent2ToolPrompt`: JSON ~200-300 tok (productCount, fields, view_mode, current_formation, data_change)

**Anthropic Prompt Caching (как работает):**
- Иерархия: `tools` → `system` → `messages` (prefix matching)
- До 4 breakpoints (`cache_control: {type: "ephemeral"}`)
- Кэш живёт 5 минут, обновляется при каждом использовании
- Запись: $1.25/MTok (×1.25 от базовой), чтение: **$0.10/MTok** (×0.1 от базовой)
- Минимум для Haiku 4.5: **4096 токенов** (ниже — кэш не работает)
- 20-block lookback window: система проверяет до 20 блоков назад от breakpoint
- Дайджест в messages — это правильно: первый запрос пишет, остальные читают за 10% цены

**Текущие breakpoints (3 из 4):**
1. Последний tool definition → кэширует все tools + padding
2. System prompt → кэширует системный промпт
3. Предпоследнее сообщение → инкрементальный кэш conversation history

**Рост истории:**
- +3 сообщения/ход (user + assistant:tool_use + user:tool_result)
- Retention обрезает до 20 сообщений (~6 ходов)
- С кэшированием: 90% истории читается за 10% цены
- tool_result маленький ("ok: found N"), tool_use средний (JSON фильтров)
- `<state>` блок — маленький (~100-200 tok мета, НЕ данные продуктов)

**Оценка без замеров:**
- Tools + padding: ~5500 tok (кэш)
- System prompt: ~500 tok (кэш)
- Digest seed: ~700 tok (кэш через messages prefix)
- 6 ходов × 3 msg: ~3000-5000 tok (инкрементальный кэш)
- Новый запрос: ~50-200 tok (не кэш)
- **Итого: ~10-12k tok, из них ~90% из кэша за 10% цены**

**Вывод:** После редизайна дайджеста проблема 80k скорее всего уже решена. Нужно подтвердить замерами:
1. Запустить бэкенд, сделать 5-6 ходов
2. Посмотреть `input_tokens`, `cache_read_input_tokens`, `cache_creation_input_tokens` в трейсах
3. Если total < 15k — шаг 3 можно закрыть как решённый побочным эффектом от шага 2

---

## Шаг 4 — Прокачка поиска + Tool Train

### Тройной гибридный поиск через tool train
**Статус:** ⏳ Не начато

**Что:** Переделать тулл поиска на tool train: одновременный запуск 3 гибридных поисков по разным направлениям (разные стратегии / запросы / фильтры).
**Зачем:** Один поиск часто промахивается. Три параллельных поиска по разным осям драматически повышают recall и precision.
**Как:** Tool train: агент формирует 3 параллельных поисковых запроса с разными стратегиями → запуск одновременно → мердж и ранжирование результатов → передача в LLM.
**Бизнес-ценность:** Точность поиска — это core value proposition. Клиент спросил "красное платье до 5000" — и нашёл. Это то, что продаёт.

---

## Шаг 5 — Визуал и кейсы

### Прокачка визуала через реальные кейсы
**Статус:** ⏳ Не начато

**Что:** Создать набор тестовых кейсов (реальные сценарии использования), прогонять их, находить и чинить проблемы визуального отображения.
**Зачем:** На демо клиент видит визуал. Кривой рендер, поехавшие карточки, обрезанные картинки = "сырой продукт".
**Как:** Автор готовит кейсы → прогон → анализ что работает / что нет → фиксы. Итеративно, через реальные сценарии.
**Бизнес-ценность:** Визуальное впечатление — второе (после скорости), что замечает клиент на демо.

---

## Шаг 6 — Инфраструктура: БД + LLM + хостинг

### Перевести БД и LLM провайдера на локальных, хостинг
**Статус:** ⏳ Не начато

**Что:** Перенести базу данных и LLM провайдера на локальные (ближние) сервера. Выбрать и поднять хостинг.
**Зачем:** Latency сети — главный враг UX. Запрос в Neon US + Anthropic API = сотни мс overhead, которые пользователь чувствует.
**Как:** БД — локальный/ближний PostgreSQL. LLM — провайдер с минимальным latency (возможно РФ провайдер или edge-инстанс). Хостинг — под реальные потребности, без оверинжиниринга.
**Бизнес-ценность:** Latency < 1s на полный цикл ответа — обязательное условие для продажи SaaS-решения. Инфраструктура должна работать, а не тормозить.

---

*Последнее обновление: 2026-02-19*
