# Doc grooming pass 1 — deprecation banners + CLAUDE.md pointer fixes

**Date:** 2026-06-01 15:20 · **Branch:** main

## Context
Vlad's doc-cadence rule (memory: feedback-doc-cadence): docs must stay
handoff-ready — "open it and hand it to developers." After the
PIM/Connector/Price-Stock decomposition and v5 going live, a lot of one_ultra
docs still describe the OLD monolith / pre-decomposition admin ingest / dropped
V4 engine as if current. This is the SAFE, non-destructive first pass.

## Approach
Ran a classification workflow (7 agents, 28 docs) judging every one_ultra doc
against the canonical current state. Kept the destructive work out: no files
moved or deleted. Applied only non-destructive deprecation banners + pointer
corrections. Physical archival + net-new canonical docs (root
SYSTEM_ARCHITECTURE.md etc.) are deferred to a later phase.

The workflow also surfaced disagreements with the earlier audit, which were
honoured (these stay untouched / current): `CATALOG_GROUP_D_SPEC.md` is current
& authoritative (Group D Phase 1 done + verified on Neon) and supersedes the
old catalog-audit; `AI_docs/agent-principles.md` is architecture-agnostic
methodology (current); `LAUNCH_ROADMAP.md` is a living doc; the three v5 ADRs
(`v5-port-from-openui`, `v5-tool-format-decision`, `v9-canvas-integration-plan`)
are current decision records.

## Files changed
- **Deprecation banners (11 docs, +2 lines each)** — `> ⚠️ HISTORICAL …`
  blockquote prepended, pointing to the canonical replacement:
  - `AI_docs/Manifesto.md`, `AI_docs/ARCHITECTURE_RULES.md`, `AI_docs/README.md`
    (pre-decomposition monolith vision/rules → root MANIFESTO.md + CLAUDE.md)
  - `docs/v5-engine-plan.md` (2026-05-03 delivery plan → FINAL_PHASE_PLAN.md)
  - `docs/catalog-audit-2026-05-07.md` (schema debt since fixed by Group D)
  - `docs/pre_launch_scenarios.md` (PARTIAL — data-plane sections describe the
    retiring discovery_v2/apply_v2 flow; auth sections still valid)
  - `docs/code_quality_audit_2026-04-27.md` (legacy project/ removed 2026-04-29)
  - `docs/bugs/E2E_RUN3_BUGS.md` (fixed bugs through 2026-03-31)
  - `docs/bugs/PENCIL_HYBRID_ENGINE_STATUS.md` (V4 engine — dropped)
  - `docs/New features/engine_v5_v9_integration_plan_2026-04-20.md` (executed)
  - `docs/New features/sales_readiness_assessment_2026-04-12.md` (V4-era)
- **`CLAUDE.md` Pointers section** — was routing agents to two stale docs as if
  current (`catalog-audit-2026-05-07` labelled "current catalog state snapshot",
  `v5-engine-plan` labelled "V5 strategic plan + remaining work"). Re-labelled
  both as HISTORICAL, annotated the two AI_docs pointers, and ADDED the missing
  canonical docs (../MANIFESTO.md, ../FINAL_PHASE_PLAN.md,
  ../SESSION_HANDOFF_2026-05-30.md, docs/PRE_LAUNCH_TASKS.md, CATALOG_GROUP_D_SPEC).
- **`README.md`** — added a non-destructive top pointer to MANIFESTO.md +
  FINAL_PHASE_PLAN.md and a one-line note that the repo is decomposing into
  focused services. README content itself left as-is (accurate, just shallow).

## Verification
- `git diff --stat`: exactly 13 files (11 banners + CLAUDE.md + README), +39/-6.
- Banners are blockquotes prepended above each doc's title; no content removed.
- Pre-existing dirty files (`docs/PRE_LAUNCH_TASKS.md`, `docs/v5-known-gaps.md`)
  were NOT touched by this pass and excluded from the commit.

## Owner decisions on the 3 uncertain docs (2026-06-01)
Vlad ruled on the docs whose status wasn't derivable from code/docs:
- `marketing_triggers_and_narration_spec` → **live idea, kept clean** (no banner).
- `OWNER_DASHBOARD_SPEC` → **live idea, kept clean** (no banner) — aligns with
  the curator "all-seeing eye" direction.
- `PITCHLINK_SPEC` → **dead → banner applied** (separate product built on the
  dropped V4 engine; V4-as-library premise needs re-validation). 12 banners total.

## Known gaps / deferred
- **Physical archival** (move banner'd docs to `docs/archive/`) deferred to the
  bigger doc-restructure phase, alongside writing the net-new canonical docs
  (root SYSTEM_ARCHITECTURE.md + DEVELOPMENT.md + one_ultra/ARCHITECTURE.md).
- Per cadence: a Friday refresh keeps this current once the target structure
  exists.
