# Phase 0 polish — batch 1 (2026-07-27)

**Context.** Owner return + interface-runtime re-anchor. Phase 0 = make the
deployed widget presentable before the operations-layer build. Source of the
gap list: the runtime-gap-map workflow (5 code readers, file:line evidence)
+ owner's live test of widget.html (three complaints, all reproduced in code).

**Branch:** `phase0-polish` (ultra) · `phase0-v5proxy-tenant` (keepstar-admin).
Not merged / not pushed — deploy on owner OK.

## Shipped in this batch

| # | Fix | Commit |
|---|---|---|
| Bug A | Grids no longer default to 3 items — replicate = all in state (cap 12), "Showing N of M" when truncated; examples de-anchored from 3 | `cee6a23` |
| Bug B | `ProductToMap` camelCase-normalises tier2/extra keys → snake_case twins collapse; duplicate facet groups gone; regression test | `cee6a23` |
| Bug C | Widget applies `/actions` result: canonical {likedIds, cartItems} state, like→unlike / add→remove toggle, optimistic update + rollback, active styles + aria-pressed | `b4dba09` |
| Price | USD-only contract (owner decision 2026-07-27, «рубли не использовать»): tool schemas declare dollars, agent1 rule + `<state>` ship dollars, storage stays cents, `formatPrice` → `$1,851` style | `711b73c` |
| Fields | `<fields>` block was silently EMPTY for all tenants (queried columns dropped 2026-05-14). Sampling rewritten to tier2/extra JSONB, camelCase-normalised, verified against live flat-moon | `75ed72e` |
| Security | admin v5 BFF proxy: tenant slug now resolved from JWT, `?tenant` is a cross-check (403 on mismatch) — closes cross-tenant read/write | admin `463409e` |

## Verification

- Go: `go build ./... && go vet ./... && go test ./internal/...` — clean (ultra backend).
- Widget: `npm run build` ok, `npm test` 102/102.
- Admin: `go build/vet/test -mod=vendor` — clean.
- New fields SQL executed against live flat-moon (hey-babes-cosmetics): returns
  rich tier2 (snake_case keys — normalisation exercised).

## Not in this batch (next)

- Digest generalisation (facets from the tenant's real tier2 key set + numeric
  ranges) — task open.
- Per-tenant `catalog_search` filter schema (replace K-beauty enums) — task open.
- Known non-goals recorded during review: RU-only guard regex + lying
  `_internal_state_filter` price path (superseded by the USD contract for now),
  no `unlike` injection engine-side (client toggles kind instead).

## Deploy note

v5-engine + admin on Railway build from GitHub `main` — merging + pushing these
branches IS the deploy. After deploy, re-test live: grid count, filter panel
dedup, heart/cart toggle, "under $50" query (should now filter correctly).
